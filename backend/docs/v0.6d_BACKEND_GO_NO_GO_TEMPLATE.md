# v0.6d 后端闭环 GO / NO-GO 判定模板

> 填表人: KimiCode（落地执行者）
> 填表时间: [待填写]
> 审计基准: `v0.6d_BACKEND_AUDIT_CHECKLIST.md`
> 项目路径: `/mnt/d/Desktop/靶向肽/github/前端/backend/`

---

## 判定总览

| 项目 | 结果 |
|:-----|:----|
| **最终判定** | [ ] GO / [ ] NO-GO |
| 后端启动 | [ ] ✅ 成功 / [ ] ❌ 失败 |
| /health | [ ] ✅ 通过 / [ ] ❌ 失败 |
| 核心端点总数 | [ ] ✅ 6/6 / [ ] ❌ 不全 |
| pytest 核心测试 | [ ] ✅ 全部通过 / [ ] ❌ 有失败 |
| 科学诚信 | [ ] ✅ 无造假 / [ ] ❌ 发现造假 |
| 前端兼容 | [ ] ✅ 未破坏 v0.6c / [ ] ❌ 已被破坏 |

---

## 一、GO 判定（所有条件须同时满足）

| # | GO 条件 | 验证方法 | 结果 | 证据 |
|:--|:-------|:--------|:----|:----|
| G.1 | 后端能启动 | `uvicorn app.main:app --host 0.0.0.0 --port 8000` | [ ] ✅ / [ ] ❌ | 启动日志: |
| G.2 | `/health` 通过 | `curl http://localhost:8000/health` → 200 | [ ] ✅ / [ ] ❌ | 响应 body: |
| G.3 | `GET /api/v1/pepmlm/candidates` 返回非空 | `curl` → data.items 非空数组 | [ ] ✅ / [ ] ❌ | items count: |
| G.4 | `GET /api/v1/amp/library` 返回非空 | `curl` → data.items 非空数组 | [ ] ✅ / [ ] ❌ | items count: |
| G.5 | `GET /api/v1/amp/structures` 返回非空 | `curl` → 200 或 404（非致命） | [ ] ✅ / [ ] ❌ | 状态码: |
| G.6 | `GET /api/v1/stamp/demo-one` 返回完整 STAMP | `curl` → 200, 含三个 domain | [ ] ✅ / [ ] ❌ | 状态码: |
| G.7 | `full_sequence` 以 `-NH2` 结尾 | 检查 `display_full_sequence` 字段 | [ ] ✅ / [ ] ❌ | display 末尾: |
| G.8 | `linker_seq = EAAAK` | 检查 `linker.sequence` 和 `linker.name` | [ ] ✅ / [ ] ❌ | 实际值: |
| G.9 | `raw_full_sequence` 不含 `-` | 检查 raw 序列无分隔符 | [ ] ✅ / [ ] ❌ | raw 首 50 字符: |
| G.10 | `validation_status = NOT_EXPERIMENTALLY_VALIDATED` | 检查响应的 validation_status | [ ] ✅ / [ ] ❌ | 实际值: |
| G.11 | `ipTM = null` | 检查 `experimental.ipTM` | [ ] ✅ / [ ] ❌ | 实际值: |
| G.12 | `pDockQ = null` | 检查 `experimental.pDockQ` | [ ] ✅ / [ ] ❌ | 实际值: |
| G.13 | `docking_score = null` | 检查 `mock_scores.docking_score` 或 mock_scores 整体为 null | [ ] ✅ / [ ] ❌ | 实际值: |
| G.14 | `interface_dG = null` | 检查 `mock_scores.binding_affinity_delta_g` 或 mock_scores 整体为 null | [ ] ✅ / [ ] ❌ | 实际值: |
| G.15 | `MIC = null` | 检查 `experimental.MIC_ug_ml` | [ ] ✅ / [ ] ❌ | 实际值: |
| G.16 | `MBC = null` | 检查 `experimental.MBC_ug_ml` | [ ] ✅ / [ ] ❌ | 实际值: |
| G.17 | 所有结构/实验指标为 null | 检查 mock_scores 为 null, experimental 所有字段均为 null | [ ] ✅ / [ ] ❌ | |
| G.18 | `is_complete = true` | 检查响应的 is_complete | [ ] ✅ / [ ] ❌ | 实际值: |
| G.19 | `orientation = N-to-C` | 检查响应的 orientation | [ ] ✅ / [ ] ❌ | 实际值: |
| G.20 | pytest 核心测试通过 | `cd backend && python -m pytest -q` | [ ] ✅ / [ ] ❌ | 通过数/失败数: |

---

## 二、NO-GO 判定（任一条件触发即止步）

| # | NO-GO 条件 | 触发? | 触发证据 |
|:--|:----------|:-----|:--------|
| N.1 | 后端无法启动（uvicorn 报错/端口占用/ImportError） | [ ] 是 / [ ] 否 | |
| N.2 | 数据文件无法读取（FileNotFoundError / JSONDecodeError） | [ ] 是 / [ ] 否 | |
| N.3 | `PepMLM candidates` 返回空数组或 500 错误 | [ ] 是 / [ ] 否 | |
| N.4 | `AMP library` 返回空数组或 500 错误 | [ ] 是 / [ ] 否 | |
| N.5 | `demo-one` 无法组装 `full_sequence`（500 或 404） | [ ] 是 / [ ] 否 | |
| N.6 | `-NH2` 出现在 full_sequence 中间（非末端） | [ ] 是 / [ ] 否 | |
| N.7 | `raw_sequence` 中出现 `-NH2` | [ ] 是 / [ ] 否 | |
| N.8 | `ipTM` 出现非 null 数值 | [ ] 是 / [ ] 否 | |
| N.9 | `pDockQ` 出现非 null 数值 | [ ] 是 / [ ] 否 | |
| N.10 | `docking_score` 出现非 null 数值 | [ ] 是 / [ ] 否 | |
| N.11 | `interface_dG / binding_affinity_delta_g` 出现非 null 数值 | [ ] 是 / [ ] 否 | |
| N.12 | `MIC / MIC_ug_ml` 出现非 null 数值 | [ ] 是 / [ ] 否 | |
| N.13 | `MBC / MBC_ug_ml` 出现非 null 数值 | [ ] 是 / [ ] 否 | |
| N.14 | 代码中包含 "verified binder" 或 "experimentally validated" 用于声称验证状态 | [ ] 是 / [ ] 否 | |
| N.15 | 后端代码修改并破坏了 v0.6c 前端页面（检查是否误动了 `src/` 或 `public/` 下的任何文件） | [ ] 是 / [ ] 否 | |
| N.16 | 缺少 `validation_status` 字段或值不是预期的 | [ ] 是 / [ ] 否 | |
| N.17 | linker 序列不是 EAAAK 或其长度不是 5 | [ ] 是 / [ ] 否 | |
| N.18 | `mock_scores` 整体非 null（即使是空 dict `{}` 也不行，必须是 `null`） | [ ] 是 / [ ] 否 | |

---

## 三、curl 验证脚本（KimiCode 执行后直接粘贴结果）

### 3.1 启动服务器

```bash
cd /mnt/d/Desktop/靶向肽/github/前端/backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 &
sleep 3
```

启动结果:
```
[粘贴 uvicorn 启动日志]
```

### 3.2 端点逐一验证

```bash
# 1. Health
curl -s http://localhost:8000/health | python3 -m json.tool
```
响应:
```json
[粘贴 /health 响应]
```

```bash
# 2. PepMLM Candidates
curl -s 'http://localhost:8000/api/v1/pepmlm/candidates?page_size=3' | python3 -m json.tool
```
响应:
```json
[粘贴 响应，截取前两条候选]
```

```bash
# 3. AMP Library
curl -s 'http://localhost:8000/api/v1/amp/library?page_size=5' | python3 -m json.tool
```
响应:
```json
[粘贴 响应]
```

```bash
# 4. AMP Structures
curl -s 'http://localhost:8000/api/v1/amp/structures?page_size=3' | python3 -m json.tool
```
响应:
```json
[粘贴 响应]
```

```bash
# 5. STAMP Demo-One (核心验收)
curl -s http://localhost:8000/api/v1/stamp/demo-one | python3 -m json.tool
```
响应:
```json
[粘贴 完整 demo-one 响应 — 这是最重要的验证项]
```

```bash
# 6. STAMP Build (手动指定)
curl -s -X POST http://localhost:8000/api/v1/stamp/build \
  -H 'Content-Type: application/json' \
  -d '{"candidate_id": "OPRF_0001"}' | python3 -m json.tool
```
响应:
```json
[粘贴 响应]
```

### 3.3 pytest 结果

```bash
cd /mnt/d/Desktop/靶向肽/github/前端/backend
python -m pytest -q --tb=short 2>&1
```
输出:
```
[粘贴 pytest 完整输出]
```

---

## 四、判定决议

### GO 路径（全部条件满足）

```
██████╗  ██████╗
██╔════╝ ██╔═══██╗
██║  ███╗██║   ██║
██║   ██║██║   ██║
╚██████╔╝╚██████╔╝
 ╚═════╝  ╚═════╝

v0.6d 后端闭环审计: GO
结论: 所有 GO 条件满足，无 NO-GO 条件触发。
下一步: 进入 v0.6d_P1_FRONTEND_INTEGRATION_PLAN.md。
```

### NO-GO 路径（任一条件失败）

```
███╗   ██╗ ██████╗       ██████╗  ██████╗
████╗  ██║██╔═══██╗     ██╔════╝ ██╔═══██╗
██╔██╗ ██║██║   ██║     ██║  ███╗██║   ██║
██║╚██╗██║██║   ██║     ██║   ██║██║   ██║
██║ ╚████║╚██████╔╝     ╚██████╔╝╚██████╔╝
╚═╝  ╚═══╝ ╚═════╝       ╚═════╝  ╚═════╝

v0.6d 后端闭环审计: NO-GO
失败项: [列出所有失败的 NO-GO 条件编号]
原因: [简述]
所需修复: [列出需要 KimiCode 修复的项目]
```

---

## 五、判定人签名

| 字段 | 内容 |
|:-----|:----|
| 执行 | KimiCode |
| 审计 | Hermes Agent (DeepSeek V4 Pro) |
| 审计时间 | 2026-04-29 01:49 PM (code review phase) |
| KimiCode 落地时间 | [待填写] |
| 最终判定 | [GO / NO-GO] |
| 附注 | [如有任何偏离或异常，在此说明] |
